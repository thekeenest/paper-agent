"""
Paper-Agent v1 — preserved linear LangGraph pipeline (Spring 2025).

All modules here are frozen; do not modify. New development lives in src/v2/.

Importing individual sub-modules (src.v1.models, src.v1.state, …) is
preferred over importing from this package directly, because some sub-modules
carry heavy optional dependencies (pandas, matplotlib, langchain).
"""

__version__ = "1.0.0"
